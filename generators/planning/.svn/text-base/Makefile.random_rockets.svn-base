TARGET = random_rockets
TARGETTYPE = App

SRCS = random_rockets_nonext.cpp

include $(SMAKE)

install : $(BuildDir)/$(TARGET)
	mv $(BuildDir)/$(TARGET) ../../../bin

all : $(BuildDir)/$(TARGET) install



